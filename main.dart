void main() {
  var giveMeFive = true;
  var numbers = [
    1,
    2,
    3,
    4,
    if (giveMeFive) 5,
    // collection if
    // giveMeFive가 true면 numbers list에 5를 추가함
  ];
  // list의 마지막에 ,(쉼표)를 붙이면 자동으로 여러 줄로 포매팅됨
}
